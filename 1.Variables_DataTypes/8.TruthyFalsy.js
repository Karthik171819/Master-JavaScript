//Falsy
let a = "";
console.log( a || "ita a Falsy value");

//Truthy
let b = "Hello";
console.log( b || "ita a Falsy value"); //hello is a truthy value, so it will be printed instead of the default message