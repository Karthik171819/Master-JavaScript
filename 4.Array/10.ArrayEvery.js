// every() method is used to check if all elements in an array satisfy a condition. It returns true if every element passes the test, otherwise false.
const n = [20, 30, 40, 45];
console.log(n.every(n => n>15));

const n1 = [ 14, 20, 30, 40, 45];
console.log(n1.every(n => n>15)); //false

